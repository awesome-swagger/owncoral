const Button = {
  // 1. We can update the base styles
  baseStyle: {
    borderRadius: 'full',
    fontWeight: 'normal',
  },
  sizes: {
    xl: {
      h: 14,
      minW: 16,
      fontSize: 'lg',
      px: 8,
    },
  },
};

export default Button;
